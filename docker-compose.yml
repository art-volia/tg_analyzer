version: "3.9"
services:
  dashboard:
    build: .
    container_name: tg-dashboard
    env_file:
      - .env
    ports:
      - "8501:8501"
    volumes:
      - ./:/app
    restart: unless-stopped

